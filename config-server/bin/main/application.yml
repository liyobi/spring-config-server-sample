
#Server port
server:
    port: 8888
 
#Git repo location.
spring:
    cloud:
        config:
            server:
                git:
                    uri: https://github.com/liyobi/spring-config-server-sample
                    timeout: 4
# Authentication basic                    
#                    username: admin
#                    password: admin
 
#Disable security of the Management endpoint
management:
    security:
        enabled: false